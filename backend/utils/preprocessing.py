# placeholder preprocessing helpers
# Add functions for encoding, scaling, imputation and a function to load saved transformers
def transform_input(input_dict):
    # Return numeric vector/ndarray in the order expected by the model
    # Implement rules used during model training
    return input_dict
